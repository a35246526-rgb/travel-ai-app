<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>여행 AI 일정 생성기 - 와이어프레임</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      padding: 20px;
      min-height: 100vh;
    }

    .container { max-width: 1400px; margin: 0 auto; }
    .app-shell { max-width: 420px; margin: 0 auto; }

    .header { text-align: center; color: white; margin: 18px 0 18px; }
    .header h1 {
      font-size: 2.0em;
      margin-bottom: 8px;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
    }
    .header p { font-size: 1.0em; opacity: 0.95; }

    /* ✅ 상단바 */
    .topbar{
      position: sticky;
      top: 0;
      z-index: 50;
      background: rgba(255,255,255,0.92);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid #eee;
      padding: 14px 16px;
      border-radius: 12px;
      margin-bottom: 14px;
      display:flex;
      align-items:center;
      justify-content: space-between;
    }
    .topbar-title{
      font-weight: 800;
      color:#4a56d6;
    }
    .topbar-btn{
      border: 0;
      background: #f3f4ff;
      color:#4a56d6;
      font-weight: 700;
      padding: 10px 12px;
      border-radius: 10px;
      cursor:pointer;
      min-width: 44px;
    }

    /* ✅ 화면 */
    .wireframe-screen{
      background: white;
      border-radius: 12px;
      padding: 22px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.2);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      display: none; /* showScreen에서 제어 */
    }

    .screen-title {
      font-size: 1.25em;
      font-weight: 800;
      color: #667eea;
      margin-bottom: 8px;
      padding-bottom: 12px;
      border-bottom: 3px solid #667eea;
    }
    .screen-subtitle { font-size: 0.9em; color: #666; margin-bottom: 16px; }

    .wireframe-element{ margin-bottom: 16px; }
    .element-label{
      font-size: 0.85em;
      font-weight: 700;
      color: #444;
      margin-bottom: 6px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .input-box{
      width: 100%;
      border: 2px solid #ddd;
      border-radius: 8px;
      padding: 12px;
      background: #f9f9f9;
      color: #333;
      font-size: 0.95em;
      margin-bottom: 10px;
    }

    .button{
      width: 100%;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 14px;
      border-radius: 8px;
      text-align: center;
      font-weight: 700;
      font-size: 1.05em;
      cursor: pointer;
      border: 0;
      box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
    }
    .button.secondary{
      background: white;
      color: #667eea;
      border: 2px solid #667eea;
      box-shadow: none;
    }

    .icon-placeholder{
      width: 50px;
      height: 50px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-radius: 50%;
      margin: 0 auto 14px;
    }

    .tag{
      display: inline-block;
      background: #e8eaf6;
      color: #667eea;
      padding: 5px 12px;
      border-radius: 20px;
      font-size: 0.85em;
      margin-right: 8px;
      margin-bottom: 8px;
      cursor: pointer;
      transition: 0.2s ease;
    }
    .tag.active {
     background: #667eea;
     color: white;
     transform: scale(1.05);
    }
    .card{
      border: 2px solid #e0e0e0;
      border-radius: 8px;
      padding: 15px;
      background: white;
      margin-bottom: 12px;
    }
    .card-header{ font-weight: 800; color: #333; margin-bottom: 8px; font-size: 1.0em; }
    .card-content{ color: #666; font-size: 0.92em; line-height: 1.6; }

    .day-card{
      background: white;
      padding: 15px;
      border-radius: 12px;
      margin-bottom: 15px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.08);
      line-height: 1.6;
      cursor: pointer;
      transition: transform 0.2s ease;
    }
    .day-card:hover{ transform: translateY(-2px); }

    .timeline-item{
      display: flex;
      gap: 12px;
      padding: 10px 0;
      border-bottom: 1px solid #eee;
    }
    .timeline-item:last-child{ border-bottom: none; }
    .timeline-time{ font-weight: 800; color: #667eea; min-width: 62px; }

    .loading-state{ text-align: center; padding: 18px; }
    .loading-spinner{
      width: 60px; height: 60px;
      border: 5px solid #f3f3f3;
      border-top: 5px solid #667eea;
      border-radius: 50%;
      margin: 0 auto 16px;
      animation: spin 1s linear infinite;
    }
    @keyframes spin { 0% { transform: rotate(0deg);} 100% { transform: rotate(360deg);} }

    .annotations{
      background: #fff9e6;
      border-left: 4px solid #ffc107;
      padding: 12px;
      margin-top: 12px;
      border-radius: 4px;
    }
    .annotation-title{ font-weight: 800; color: #f57c00; margin-bottom: 6px; font-size: 0.9em; }
    .annotation-list{ font-size: 0.85em; color: #666; line-height: 1.6; }
    .annotation-list li{ margin-bottom: 4px; margin-left: 20px; }
  
  /* ✅ 하단 고정 버튼 */
.bottom-fixed {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  background: white;
  padding: 14px 20px;
  box-shadow: 0 -4px 20px rgba(0,0,0,0.1);
}

.bottom-fixed .button {
  max-width: 420px;
  margin: 0 auto;
  display: block;
  border-radius: 14px;
}

  </style>
</head>

<body>
  <div class="container">
    <div class="app-shell">

      <!-- ✅ 상단바는 하나만 -->
      <div class="topbar" id="topbar">
        <button class="topbar-btn" id="topbarBack" onclick="handleTopbarBack()" style="visibility:hidden;">←</button>
        <div class="topbar-title" id="topbarTitle">입력</div>
        <div style="width:44px;"></div>
      </div>

      <div class="header">
        <h1>✈️ 여행 AI 일정 생성기</h1>
        <p>UI/UX 와이어프레임 디자인</p>
      </div>

      <!-- ✅ Screen 1 -->
       <div class="bottom-fixed" id="bottomButton">
  <button class="button" onclick="generateItinerary()">
    🤖 AI 일정 생성하기
  </button>
</div>

      <div class="wireframe-screen" id="inputScreen">
        <div class="screen-title">1️⃣ 입력 폼 화면</div>
        <div class="screen-subtitle">여행 정보 입력 페이지</div>

        <div class="icon-placeholder"></div>

        <div class="wireframe-element">
          <div class="element-label">여행지</div>
          <input id="location" class="input-box" placeholder="예: 제주도" />
          <input id="days" class="input-box" placeholder="예: 3" />
          <input id="style" class="input-box" placeholder="예: 맛집/자연/힐링" />
        </div>

        <div class="wireframe-element">
          <div class="element-label">여행 스타일</div>
          <div>
            <span class="tag">문화 체험</span>
            <span class="tag">맛집 탐방</span>
            <span class="tag">쇼핑</span>
          </div>
        </div>

        <div class="annotations">
          <div class="annotation-title">📝 디자인 노트</div>
          <ul class="annotation-list">
            <li>입력값 검증(빈 값/일수)</li>
            <li>결과 → 상세 화면 전환</li>
            <li>상단바 제목/뒤로가기 자동 변경</li>
          </ul>
        </div>
      </div>

      <!-- ✅ Screen 2 -->
      <div class="wireframe-screen" id="loadingScreen">
        <div class="screen-title">2️⃣ 로딩 화면</div>
        <div class="screen-subtitle">AI 일정 생성 중</div>

        <div class="loading-state">
          <div class="loading-spinner"></div>
          <div style="font-weight: 800; color: #667eea; font-size: 1.05em; margin-bottom: 8px;">
            AI가 맞춤 일정을 생성하고 있습니다
          </div>
          <div style="color: #666; font-size: 0.9em;">잠시만 기다려 주세요...</div>
        </div>

        <div class="card">
          <div class="card-header">🔍 분석 중인 요소들</div>
          <div class="card-content">여행지 인기 명소 · 최적 동선 · 맛집 추천 · 예산 최적화</div>
        </div>
      </div>

      <!-- ✅ Screen 3 -->
      <div class="wireframe-screen" id="resultScreen">
        <div class="screen-title">3️⃣ 결과 화면</div>
        <div class="screen-subtitle">생성된 여행 일정</div>

        <div class="wireframe-element">
          <button class="button secondary" onclick="goBack()">← 다시 입력하기</button>
        </div>

        <div id="result"></div>
      </div>

      <!-- ✅ Screen 4 -->
      <div class="wireframe-screen" id="detailScreen">
        <div class="screen-title">📌 Day 상세</div>
        <div class="screen-subtitle" id="detailSubtitle">선택한 Day의 상세 일정</div>

        <div class="wireframe-element">
          <button class="button secondary" onclick="backToResult()">← 결과로 돌아가기</button>
        </div>

        <div id="detailContent"></div>
      </div>

    </div>
  </div>

<script>
  // ✅ 제주 데이터 + 랜덤
  const jejuData = {
    관광지: ["성산일출봉","한라산","협재해변","우도","용두암","섭지코지"],
    맛집: ["동문시장","흑돼지 거리","갈치조림 맛집","고기국수집","해물뚝배기 전문점"],
    카페: ["애월 카페거리","오션뷰 카페","감성 브런치 카페","돌담 감성 카페"]
  };
  function getRandomItem(arr){ return arr[Math.floor(Math.random()*arr.length)]; }

  // ✅ 전역 상태
  let itineraryData = [];
  let currentScreen = "inputScreen";

  // ✅ 상단바 제어
  function setTopbar(screenId){
    const titleEl = document.getElementById("topbarTitle");
    const backBtn  = document.getElementById("topbarBack");

    const titles = {
      inputScreen: "입력",
      loadingScreen: "생성 중…",
      resultScreen: "결과",
      detailScreen: "상세"
    };

    if (titleEl) titleEl.textContent = titles[screenId] || "여행 AI";
    if (backBtn){
      backBtn.style.visibility =
        (screenId === "inputScreen" || screenId === "loadingScreen") ? "hidden" : "visible";
    }
  }

  // ✅ 화면 전환(이게 없어서 입력창이 안 뜬 거!)
  function showScreen(screenId){
    currentScreen = screenId;

    ["inputScreen","loadingScreen","resultScreen","detailScreen"].forEach(id=>{
      const el = document.getElementById(id);
      if (el) el.style.display = "none";
    });

    const target = document.getElementById(screenId);
    if (target) target.style.display = "block";

    setTopbar(screenId);
    // 하단 버튼 제어
const bottomBtn = document.getElementById("bottomButton");
if (screenId === "inputScreen") {
  bottomBtn.style.display = "block";
} else {
  bottomBtn.style.display = "none";
}

  }

  // ✅ 상단바 뒤로가기
  function handleTopbarBack(){
    if (currentScreen === "resultScreen") goBack();
    else if (currentScreen === "detailScreen") backToResult();
  }

  function generateItinerary() {
    const location = document.getElementById("location").value.trim();
    const days = parseInt(document.getElementById("days").value, 10);
    const styleInput = document.getElementById("style").value.trim();

    if (!location || !days || days < 1) {
      alert("여행지와 여행일수를 제대로 입력해줘!");
      return;
    }

    showScreen("loadingScreen");

    setTimeout(() => {
      itineraryData = Array.from({ length: days }, (_, idx) => {
        const day = idx + 1;

        const style = styleInput.replace(/\s+/g, "");
        let items = [];

        if (style.includes("맛집")) {
          items = [
            { time: "09:00", text: `☕ ${getRandomItem(jejuData.카페)}` },
            { time: "11:00", text: `🍜 ${getRandomItem(jejuData.맛집)}` },
            { time: "13:00", text: `🍜 ${getRandomItem(jejuData.맛집)}` },
            { time: "15:00", text: `📍 ${getRandomItem(jejuData.관광지)}` },
            { time: "19:00", text: `🍜 ${getRandomItem(jejuData.맛집)}` }
          ];
        } else if (style.includes("힐링") || style.includes("자연")) {
          items = [
            { time: "09:00", text: `🌿 ${getRandomItem(jejuData.관광지)}` },
            { time: "11:00", text: `☕ ${getRandomItem(jejuData.카페)}` },
            { time: "13:00", text: `🍜 ${getRandomItem(jejuData.맛집)}` },
            { time: "15:00", text: `🌿 ${getRandomItem(jejuData.관광지)}` },
            { time: "19:00", text: `☕ ${getRandomItem(jejuData.카페)}` }
          ];
        } else {
          items = [
            { time: "09:00", text: `☕ ${getRandomItem(jejuData.카페)}` },
            { time: "11:00", text: `📍 ${getRandomItem(jejuData.관광지)}` },
            { time: "13:00", text: `🍜 ${getRandomItem(jejuData.맛집)}` },
            { time: "15:00", text: `📍 ${getRandomItem(jejuData.관광지)}` },
            { time: "19:00", text: `🌙 ${getRandomItem(jejuData.카페)}` }
          ];
        }

        return {
          day,
          title: `${location} Day ${day}`,
          items,
          tip: "💡 팁: 가까운 지역끼리 묶으면 이동 시간이 줄어!"
        };
      });

      const resultDiv = document.getElementById("result");
      let html = `<h2 style="margin:10px 0 14px;">${location} ${days}일 여행 일정</h2>`;

      itineraryData.forEach(d => {
        html += `
          <div class="day-card" onclick="openDayDetail(${d.day})">
            <h3 style="margin-bottom:6px;">Day ${d.day}</h3>
            ${d.items.slice(0, 4).map(x => x.text).join("<br>")}
            <div style="margin-top:10px; font-size:0.9em; color:#667eea; font-weight:800;">
              👉 눌러서 상세 보기
            </div>
          </div>
        `;
      });

      resultDiv.innerHTML = html;
      showScreen("resultScreen");
    }, 1200);
  }

  function openDayDetail(dayNumber){
    const day = itineraryData.find(d => d.day === dayNumber);
    if (!day) return;

    document.getElementById("detailSubtitle").innerText = `${day.title} 상세 일정`;

    document.getElementById("detailContent").innerHTML = `
      <div class="card">
        <div class="card-header">🗓️ Day ${day.day} 타임라인</div>
        <div class="card-content">
          ${day.items.map(item => `
            <div class="timeline-item">
              <div class="timeline-time">${item.time}</div>
              <div>${item.text}</div>
            </div>
          `).join("")}
        </div>
      </div>
      <div class="card" style="background:#e8f5e9; border-color:#4caf50;">
        <div class="card-header" style="color:#2e7d32;">오늘의 팁</div>
        <div class="card-content" style="color:#2e7d32;">${day.tip}</div>
      </div>
    `;

    showScreen("detailScreen");
  }

  function backToResult(){ showScreen("resultScreen"); }

  function goBack(){
    showScreen("inputScreen");
    document.getElementById("result").innerHTML = "";
    itineraryData = [];
    document.getElementById("location").value = "";
    document.getElementById("days").value = "";
    document.getElementById("style").value = "";
  }

  // ✅ 중요: 첫 화면 띄우기 (이게 없어서 입력창이 안 뜬 거)
  showScreen("inputScreen");
  // ✅ 태그 복수 선택 기능
const tags = document.querySelectorAll(".tag");
const styleInput = document.getElementById("style");

function syncStyleInput() {
  const selected = Array.from(tags)
    .filter(tag => tag.classList.contains("active"))
    .map(tag => tag.innerText.trim());

  styleInput.value = selected.join(", ");
}

tags.forEach(tag => {
  tag.addEventListener("click", () => {
    // 클릭하면 토글(선택/해제)
    tag.classList.toggle("active");
    syncStyleInput();
  });
});

// ✅ 사용자가 직접 입력창을 수정했을 때도, 태그 상태 반영하고 싶으면(선택사항)
// styleInput.addEventListener("input", () => {
//   const text = styleInput.value.replace(/\s+/g, "");
//   tags.forEach(tag => {
//     const name = tag.innerText.replace(/\s+/g, "");
//     if (text.includes(name)) tag.classList.add("active");
//     else tag.classList.remove("active");
//   });
// });

</script> 
</body>
</html>

